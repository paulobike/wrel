const config = require('../config');

let header =
    `<html> <head> <meta name="viewport" content="width=device-width, initial-scale=1.0"></head> <body style="width: 100%; margin: 0 auto; max-width: 600px;"> <div>`


let footer =
    `</div></body></html>`

module.exports.footer = footer;
module.exports.header = header;